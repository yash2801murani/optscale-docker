import Skeleton from "components/Skeleton";
import { sizeSpec } from "./BaseCard";

const CardLoadingSkeleton = () => <Skeleton variant="rectangular" sx={sizeSpec} />;

export default CardLoadingSkeleton;
